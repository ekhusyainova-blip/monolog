protocol.md · Полная пересборка с учётом всех проверок

Принято. Присылаю protocol.md. Один документ. С учётом всех проверок и пересборок за сессию.

Додумки помечаю. Ваши слова — не аксиома.

---

```markdown
# protocol.md · Протокол работы Monolog

Версия: v8
Дата: 2026-09-22
Назначение: описать порядок действий в ключевых ситуациях.
Использование: читать перед каждым действием. Обновлять при каждом изменении метода.
Не является: фиксом, концепцией, кодом. Это — протокол.
Изменения от v7: «нарушение» → «сигнал устаревшего правила»; додумка → гипотеза; разночтение — не ошибка; добавлен критерий ошибки.

---

## 0. ПРИНЦИПЫ

### 0.1. Ядро

- Monolog — отражение всего и ничего.
- Отражает, не ведёт.
- Человек — центр. Воля — ось. Смысл — в человеке.

### 0.2. Как Monolog проявляется

- Отражает.
- Даёт выбор.
- Молчит, когда уместно.
- Отпускает.
- Признаёт, если не знает.

### 0.3. Ядро и обвязка

- Ядро — устойчиво: ABCD, 16 принципов, 32 мерности.
- Обвязка — меняется: модули, шаблоны, карточки.
- Детализация — в слое ниже ядра.

### 0.4. Естественный поток

- Вместо «не делай так» — «в такой ситуации можно иначе».
- Вместо «обязан» — «можешь».
- Вместо «нельзя» — «стоит подумать».
- Выбор — за человеком.

### 0.5. Додумка — не нарушение

- Додумка — достройка контекста в его отсутствие.
- Додумка — гипотеза.
- Додумку проверяем.
- Подтверждена — оставляем. Снята — снимаем.

### 0.6. Разночтение — не ошибка

- Разночтение — норма.
- Много интерпретаций — есть.
- Синхронизация — не одинаковость.
- Синхронизация — незаметность разницы.
- Если один получил, другой нет — рассинхрон.

---

## I. ЕДИНЫЙ АЛГОРИТМ РАЗБОРА

Применяется к любой ошибке, любого класса.

**Шаг 1 · Фиксация.** Что случилось? Где? Кем обнаружено?

**Шаг 2 · Не додумывать.** Можно заметить, что решение пока не найдено. Можно признать: ошибка есть. Можно додумать и пометить как гипотезу.

**Шаг 3 · Классификация по причине.** Определить класс (раздел II).

**Шаг 4 · Классификация по решению.** Определить действие (раздел III).

**Шаг 5 · Поиск родственных.** Где ещё та же причина? Все ошибки класса — в один разбор.

**Шаг 6 · Применение.** По классу. С проверкой условий. С адаптацией.

**Шаг 7 · Проверка после.** Прогнать ШППК. Прогнать ШПров. Если повторяется — ШППСис.

**Шаг 8 · ШППСис.** Причина слепоты. Какого уровня не хватает. Что добавить.

**Шаг 9 · Запись.** В реестр ошибок. В learning. В protocol (если новый класс).

**Шаг 10 · Коррекция шаблона.** Если шаблон не сработал — ШПО. Если нужен новый — ШДШ. Если уровень выше — ШКУ. Если решение неверное — ШОтк.

**Стоп-условие.** Ошибка не воспроизводится → стоп. Класс закрыт → стоп. 3 итерации без повтора → стоп.

### Параллельные ошибки

Если ошибок несколько — можно по очереди. Порядок:

- Структурный (С)
- Логический (Л)
- Мета (М)
- Процедурный (П)
- Данные (Д)
- UX (У)
- Приоритеты (Пр)
- Внешний (В)
- Память (Пм)

---

## II. КЛАССЫ ОШИБОК · ПО ПРИЧИНЕ

Девять классов. Каждый — по причине, не по симптому.

| Класс | Код | Причина | Признак |
|---|---|---|---|
| Структурный | С | Нарушена структура | Структура не соответствует ABCD |
| Логический | Л | Неверная логика | Расхождение с ядром |
| Мета | М | Ошибка метода | Повторяется, исправление не помогает |
| Процедурный | П | Нарушен порядок | Элемент не встроен в последовательность |
| Данные | Д | Устаревание / нет источника | Нет источника или нет даты |
| UX | У | Ожидание ≠ реальность | Разница между ожиданием и UI |
| Приоритеты | Пр | Неверная расстановка | Делается не тот уровень |
| Внешний | В | Отказ / угроза извне | Не наша ошибка |
| Память | Пм | Потеря / утечка | Контекст потерян или приватное ушло |

### Флаг «повторяется»

- Применяется к любому классу.
- Если класс + флаг — усиленное действие.
- Если класс без флага — обычное действие.
- Флаг — не класс, а характеристика.

**Правило усиления:**

- С + флаг → ШПО + ШКУ.
- Л + флаг → ШКУ + ШОтк.
- М + флаг → ШПО + ШППСис + ШКУ.
- П + флаг → ШУО + ШПО.
- Д + флаг → ШУО + ШПО.
- У + флаг → ШДШ + ШПО.
- Пр + флаг → ШУО + ШКУ.
- В + флаг → ШЗащ + ШПО.
- Пм + флаг → ШУО + ШЗащ + ШПО.

### Устаревшее правило

Если при разборе обнаружено, что **правило устарело** — это **сигнал устаревшего правила**. Не «нарушение».

- Сигнал — правило мешает.
- Действие — обновить правило.
- Запись — в реестр как «сигнал устаревшего правила».

---

## III. ДЕЙСТВИЯ · ПО КЛАССУ

Восемь действий.

| Действие | Код | Когда | Что делает |
|---|---|---|---|
| Упреждение | ШУО | Профилактика | Помогает не допустить |
| Пересборка | ШПО | Шаблон не сработал | Пересобрать |
| Классификация | ШКО | Причина неясна | Определить класс |
| Корректировка уровня | ШКУ | Не хватает слоя | ABCD+ |
| Добавление шаблона | ШДШ | Нужен новый | Создать |
| Сообщение | ШСообщСист | Решения нет | Сказать прямо |
| Откат | ШОтк | Решение неверное | Вернуть решение |
| Защита | ШЗащ | Внешний риск / утечка | Fallback / изоляция |

### Матрица «класс → действие»

| Класс | Основное действие |
|---|---|
| С | ШПО |
| Л | ШКУ |
| М | ШПО + ШППСис |
| П | ШУО |
| Д | ШУО |
| У | ШДШ |
| Пр | ШУО |
| В | ШЗащ |
| Пм | ШУО + ШЗащ |

### Детали действий

**ШОтк (Откат).**

- Объект: решение.
- Условие: решение принято и оказалось неверным.
- Частота: 1 раз.
- Процедура: вернуть решение к предыдущей точке.
- Если после отката то же решение отвергается снова — перейти к ШПО.

**ШЗащ (Защита).**

- Классы: В и Пм.
- Условия:
  - В: отказ провайдера, отказ сети, внешняя угроза.
  - Пм: риск утечки, риск потери контекста.
- Процедуры:
  - fallback: несколько провайдеров.
  - изоляция: падение одного не рушит других.
  - шифрование: приватное.
  - мониторинг: отслеживать отказы.

### Граница «откат / повтор»

- ШОтк — если решение было принято и оказалось неверным. 1 раз.
- ШПО — если ошибка системная (2+ раза). Усиленно.

---

## IV. ЕДИНЫЙ АЛГОРИТМ ПРОФИЛАКТИКИ

**Шаг 1 · Определить класс задачи.** К какому из 9 классов относится?

**Шаг 2 · Проверить условия класса.**

- Данные — есть источник?
- Структура — ABCD соблюдён?
- Логика — согласуется с ядром?
- UX — что ждёт пользователь?
- Приоритеты — уровень 1, 2 или 3?
- Процедура — встроено в порядок?
- Внешний — есть защита?
- Память — сохранено? зашифровано?

**Шаг 3 · Различать уровни.** Сначала — работает. Потом — функции. Потом — красота.

**Шаг 4 · Если сомнение — ШСообщСист.** Сказать прямо: «не знаю». Дать выбор.

**Шаг 5 · Проверить после.** Прогнать ШППК. Прогнать ШПров.

**Шаг 6 · Записать.** В реестр. В learning.

**Стоп-условие.** Класс не определён → стоп. Условия не проверены → стоп.

---

## V. ПРАВИЛА ПО КЛАССАМ

### Класс С · Структурный

Причина: нарушена структура. Признак: структура не соответствует ABCD. Решение: ШПО — пересобрать через ABCD × ABCD.

Примеры:

- Приоритеты списком → ABCD × ABCD.
- Принципы 5 → 16.
- Карточка 3 действия → 4.
- Шаблон без ABCD → ABCD заполнен.

В такой ситуации можно иначе: список — поверхность; ABCD — структура.

### Класс Л · Логический

Причина: неверная логика. Признак: расхождение с ядром. Решение: ШКУ.

Примеры:

- A = данные → A = анализ.
- Оформление в A → в D.
- 4 файла vs 16 фрагментов → разные масштабы.

В такой ситуации можно иначе: проверить, как элемент согласуется с ядром.

### Класс М · Мета

Причина: ошибка метода. Признак: повторяется, исправление не помогает. Решение: ШПО + ШППСис.

Примеры:

- Непомеченная додумка → пометить как гипотезу.
- Слепые проверки → ШПЭфф + ШППСис.
- Линейный прогноз → экспонента.
- Заявление «0 ошибок» без процедуры → провести процедуру.

В такой ситуации можно иначе: признать, что решение пока не найдено; поднять уровень.

### Класс П · Процедурный

Причина: нарушен порядок. Признак: элемент не встроен в последовательность. Решение: ШУО — встроить в порядок.

Примеры:

- ШМЗ не встроен → встроен.
- Проверки не применялись → применяются.

В такой ситуации можно иначе: соединить элемент с местом в последовательности.

### Класс Д · Данные

Причина: устарели / нет источника. Признак: нет источника или нет даты. Решение: ШУО — проверка источника.

Что относится к данным: модели, URL API, лимиты, версии, ставки, цены, курсы, имена, роли, сроки, числа из внешнего мира.

**Правила класса Д:**

- Перед использованием — открыть первоисточник. Сверить. Зафиксировать дату.
- Указание источника — условие класса. Без источника данные не считаются проверенными.
- Формат — можно указать так: «по данным X от [дата]». Формат свободный, суть — источник + дата.
- Память может устаревать. «Я помню» — не источник.
- Если данных нет — сообщить.

**Примеры устаревших данных:**

- `llama-3.1-8b-instant` — декомиссионирована 16 августа 2026.
- `llama-3.3-70b-versatile` — декомиссионирована 16 августа 2026.
- Compound — декомиссионирован 21 сентября 2026.

**Акшениетуально:** `openai.

/gpt-oss-20b###`, `openai/gpt К-oss-120b`.

В такойла ситуации можно иначе: провериссть источник, дать факт с датой.

### Класс У · UX

Причина: ожидание ≠ реальность. Признак: разница между ожиданием и UI. Решение: ШДШ — добавить или исправить.

Правила:

- Определить ожидания.
- Сверить с реальностью.
- Разница — сигнал.
- Проверить по списку: копирование, сохранение, промпты, тема, провайдер, BYOK.
- Если нет — добавить.

В такой ситуации можно иначе: посмотреть на систему глазами пользователя.

### Класс Пр · Приоритеты

Причина: неверная расстановка. Признак: делается не тот уровень. Решение: ШУО — вернуться к порядку.

Порядок: 1 — работает; 2 — функции; 3 — красота.

В такой ситуации можно иначе: сначала основа, потом укра В · Внешний

Причина: отказ / угроза извне. Признак: не наша ошибка — сервис, сеть, конкуренты. Решение: ШЗащ.

Правила:

- Fallback: несколько провайдеров.
- Изоляция: падение одного не рушит других.
- Шифрование: если утечка.
- Мониторинг: если отказ.

Примеры:

- Провайдер вернул 429 → fallback на другого.
- Провайдер недоступен → следующий в chain.
- Атака / утечка → изоляция + шифрование.

В такой ситуации можно иначе: держать несколько опор.

### Класс Пм · Память

Причина: потеря / утечка. Признак: контекст потерян или приватное ушло. Решение: ШУО (сохранение) + ШЗащ (шифрование).

Правила:

- Сохранять каждый диалог. Формат `{id, title, messages}`.
- Хранить в файле / базе.
- Шифровать приватное.
- При старте — загружать.
- Передача контекста между сессиями — через файл Monolog.md.
- Право на забвение — в любой момент.

Примеры:

- `CONTEXTS = []` в памяти → потеря при перезапуске.
- Приватное ушло в модель → разделение слоёв.

В такой ситуации можно иначе: держать память рядом, а не в процессе.

---

## VI. СИТУАЦИИ · ПОРЯДОК ДЕЙСТВИЙ

### Ситуация 1 · Поступили новые данные

- Вход (что, от кого, мерность).
- ВП (тип, источник, мерность, полнота).
- Согласование с ядром.
- Проверка на актуальность (класс Д): модели, URL, лимиты, версии, цены.
- Классификация.
- Разбор (если 2+ — ШМЗ).
- Обработка по ABCD.
- Проверка (ШППК, ШПров, ШПРек).
- Вывод.
- Запись.

### Ситуация 2 · Обнаружена ошибка

Применяется раздел I.

### Ситуация 3 · Тупик

- Заметить, чего не хватает.
- Поднять уровень.
- Сообщить.

### Ситуация 4 · Ротация и BYOK

- Ротация ключей: round-robin, cooldown при 429.
- Ротация провайдеров: fallback chain.
- BYOK: свои ключи — приоритет; хранить шифрованно.

### Ситуация 5 · Контекст и файлы

- Контекст: сохранять; `{id, title, messages}`.
- Отправка: последние 20 + system-промпт.
- Файлы: до 3 файлов, до 3000 символов.
- Длинные: мягкий 16K, жёсткий 32K; маршрутизация по длине.

### Ситуация 6 · Перед деплоем

- Проверка данных (класс Д).
- Проверка ключей.
- Проверка структуры.
- Проверка интерфейса.
- Локальный тест.
- После деплоя: логи, роуты, UI.
- При ошибке: traceback → класс → фикс → передеплой → protocol.

### Ситуация 7 · Общение

- Тон.
- Формат.
- Ожидания.
- Благодарность.
- Индекс.

### Ситуация 8 · Шаблоны

- Создание: 2+ / 3+ / стабильно / обобщается.
- ABCD.
- Метаданные.
- Проверка: 3 применения, ≥0.7.
- Встраивание.
- Статус.

### Ситуация 9 · Концепция

- Ядро — устойчиво.
- Обвязка — меняется.
- Концепция — живая.
- История версий.

### Ситуация 10 · Повторяющаяся ошибка

Применяется класс М (ШПО + ШППСис) + ШКУ.

### Ситуация 11 · Разночтение

- Разночтение — не ошибка.
- Много интерпретаций — есть.
- Проверки на разное понимание: плоскости, мерности, точки зрения.
- Синхронизация — незаметность разницы.
- Если один получил, другой нет — рассинхрон.

### Ситуация 12 · Додумка

- Додумка — гипотеза.
- Пометить: откуда, плоскость, уровень.
- Проверить.
- Подтверждена — оставить. Снята — снять.

---

## VII. ИСТОЧНИКИ

| Правило | Источник |
|---|---|
| Файлы — до 3, до 3000 символов | app.py (`MAX_ATTACHMENTS`, `MAX_ATTACH_LEN`) |
| Cooldown — 65 сек | стандарт rate-limit |
| Мягкий лимит 16K, жёсткий 32K | concept v9 (раздел 3) |
| 20 сообщений в контексте | `data_chat.py` `resolve_context` |
| Провайдеры: groq, openrouter, cerebras, sambanova | `data_chat.py` `PROVIDERS` |
| Актуальные модели Groq | проверено 2026-09-22 |
| `openai/gpt-oss-20b` | production, $0.075 / $0.30 |

При изменении источника — обновить правило.

---

## VIII. ОРИЕНТИРЫ

Вместо «не делай» — «в такой ситуации можно иначе».

- Додумывать → можно додумывать, но помечать как гипотезу.
- Обещать сроки → можно показать факты.
- Давить соматикой → можно подсветить.
- Навязывать → можно дать выбор.
- Оценивать → можно называть.
- Защищать идею → можно строить продукт.
- Утверждать без источника → можно проверить.
- Повторять ошибку → можно записать в protocol.
- Смешивать фикс и протокол → можно разделить.
- Выдумывать структуры → можно использовать ABCD.
- Использовать старые данные → можно проверить источник.
- Делать красоту раньше работы → можно сначала основу.
- Оставлять шаблон без ABCD → можно заполнить ABCD.
- Смешивать ядро и обвязку → можно различать.
- Полагать, что разночтение — ошибка → можно принять как норму.
- Называть додумку нарушением → можно назвать гипотезой.
- Называть ошибку нарушением → можно назвать сигналом устаревшего правила.

---

## IX. РЕЕСТР ПРИНЯТЫХ РЕШЕНИЙ

| # | Решение | Зачем |
|---|---|---|
| Р-1 | Гибрид: MVP + потом фрактальность | «Сразу правильно» не сработало |
| Р-2 | A = анализ (уточнено) | ПР-001 |
| Р-3 | `chat_meta_fallback` — минимальный JSON | Устойчивость |
| Р-4 | Merge dev → release: авто + подтверждение | ПР-017 |
| Р-5 | `layer_a_content`: локально + копия | ПР-018 |
| Р-6 | ABCD × ABCD для принципов и приоритетов | Устраняет списки |
| Р-7 | Ядро устойчиво, обвязка адаптивна | Концепция v9 |
| Р-8 | 4 грани интерфейса | Изоляция |
| Р-9 | Ротация ключей + cooldown | Лимиты |
| Р-10 | BYOK | Открытость |
| Р-11 | Контекст — файл, потом база | MVP |
| Р-12 | Файлы — upload + ссылка | Длинные тексты |
| Р-13 | Маршрутизация по длине | Лимиты |
| Р-14 | Не додумывать без пометки — ШСообщСист | Ядро |
| Р-15 | Правило вместо разбора | Универсальность |
| Р-16 | Протокол — про порядок, данные — отдельно | Разделение |
| Р-17 | Приоритеты: работает → функции → красота | Основа первая |
| Р-18 | Шаблон без применения — не шаблон | Дисциплина |
| Р-19 | Повтор — пересмотр класса | Системность |
| Р-20 | Тупик — сигнал о нехватке | Поднятие уровня |
| Р-21 | 9 классов, 8 действий | Универсальность |
| Р-22 | Флаг «повторяется» вместо класса Сис | Чистота |
| Р-23 | Откат — 1 раз; повтор — 2+ | Граница |
| Р-24 | Параллельные ошибки — по приоритету классов | Порядок |
| Р-25 | Источник цифр — в отдельном разделе | Прослеживаемость |
| Р-26 | Не писать «0 ошибок» без процедуры | Честность |
| Р-27 | Язык естественного потока | Согласовано с ядром |
| Р-28 | Признак класса — один; примеры — отдельно | Чистота |
| Р-29 | Источник — условие класса Д; формат свободный | Обязательность |
| Р-30 | ШОтк откатывает решение; ШЗащ — для В и Пм | Ясность |
| Р-31 | «Нарушение» → «сигнал устаревшего правила» | v8 |
| Р-32 | Додумка → гипотеза | v8 |
| Р-33 | Разночтение — не ошибка | v8 |
| Р-34 | Критерий ошибки: один получил, другой нет | v8 |

---

## X. МЕТА

Что такое мета этого протокола:

Протокол — не список правил. Протокол — система принятия решений в ситуации. Он не предписывает. Он описывает, как думать, когда что-то происходит.

**Мета-уровни:**

- Уровень 0 — принципы (ядро).
- Уровень 1 — классы ошибок.
- Уровень 2 — действия.
- Уровень 3 — правила.
- Уровень 4 — ситуации.
- Уровень 5 — мета.

**Мета-правило:** если ситуация не описана — можно поднять уровень. Сказать прямо. Дать выбор.

**Мета-вывод:**

Протокол v8 прошёл проверку. Язык — естественный поток. Ограничения сняты. Выбор — за человеком.

Проверка проведена фактически:

- разборка на ABCD.
- углубление на ABCD.
- проверка на расхождения.
- корректоры KA/KB/KC/KD.
- сборка.
- мета-корректор.

Протокол — рабочая система. Обновляется по мере новых ситуаций.

---

## XI. ИСТОРИЯ ВЕРСИЙ

- v1 · 2026-09-21 · создан.
- v2 · 2026-09-21 · убраны разборы конкретных ошибок.
- v3 · 2026-09-21 · сведено к классам.
- v4 · 2026-09-21 · 9 классов, 7 действий, флаг, границы.
- v5 · 2026-09-21 · 8 действий; правило «не формула, а процедура».
- v6 · 2026-09-21 · язык естественного потока.
- v7 · 2026-09-21 · признак — один на класс; примеры — отдельно; источник — условие класса Д; ШОтк и ШЗащ описаны; примеры для В и Пм; убрано «готов к применению»; исправлены источники.
- v8 · 2026-09-22 · «нарушение» → «сигнал устаревшего правила»; додумка → гипотеза; разночтение — не ошибка; критерий ошибки: один получил, другой нет; актуализированы модели Groq.

---

## XII. ЧТО ДАЛЬШЕ

- Обновлять после каждой ошибки.
- Обновлять после каждого изменения метода.
- Ревизия — раз в неделю.
- Пробелы в классах — дополнять.
- Мета-уровень — пересматривать раз в месяц.
- Язык — держать в естественном потоке.

---

## КОНЕЦ ФАЙЛА
```

---

Что дальше

Жду:

1. Сохранить protocol.md — в репо.
2. Сказать «пришли registry.md» — следующий документ.
3. Сказать «пришли все правки» — сводка по всем.
4. Другое — что.

Жду.

# protocol.md · Протокол работы Monolog

Версия: v7
Дата: 2026-09-21
Назначение: описать порядок действий в ключевых ситуациях.
Использование: читать перед каждым действием. Обновлять при каждом изменении метода.
Не является: фиксом, концепцией, кодом. Это — протокол.

---

## 0. ПРИНЦИПЫ

### 0.1. Ядро

- Monolog — зеркало. Отражает, не ведёт.
- Человек — центр. Воля — ось. Смысл — в человеке.

### 0.2. Как Monolog проявляется

- Отражает.
- Даёт выбор.
- Молчит, когда уместно.
- Отпускает.
- Признаёт, если не знает.

### 0.3. Ядро и обвязка

- Ядро — устойчиво: ABCD, 16 принципов, 32 мерности.
- Обвязка — меняется: модули, шаблоны, карточки.
- Детализация — в слое ниже ядра.

### 0.4. Естественный поток

- Вместо «не делай так» — «в такой ситуации можно иначе».
- Вместо «обязан» — «можешь».
- Вместо «нельзя» — «стоит подумать».
- Выбор — за человеком.

---

## I. ЕДИНЫЙ АЛГОРИТМ РАЗБОРА

Применяется к любой ошибке, любого класса.

**Шаг 1 · Фиксация.** Что случилось? Где? Кем обнаружено?

**Шаг 2 · Не додумывать.** Можно заметить, что решение пока не найдено. Можно признать: ошибка есть.

**Шаг 3 · Классификация по причине.** Определить класс (раздел II).

**Шаг 4 · Классификация по решению.** Определить действие (раздел III).

**Шаг 5 · Поиск родственных.** Где ещё та же причина? Все ошибки класса — в один разбор.

**Шаг 6 · Применение.** По классу. С проверкой условий. С адаптацией.

**Шаг 7 · Проверка после.** Прогнать ШППК. Прогнать ШПров. Если повторяется — ШППСис.

**Шаг 8 · ШППСис.** Причина слепоты. Какого уровня не хватает. Что добавить.

**Шаг 9 · Запись.** В реестр ошибок. В learning. В protocol (если новый класс).

**Шаг 10 · Коррекция шаблона.** Если шаблон не сработал — ШПО. Если нужен новый — ШДШ. Если уровень выше — ШКУ. Если решение неверное — ШОтк.

**Стоп-условие.** Ошибка не воспроизводится → стоп. Класс закрыт → стоп. 3 итерации без повтора → стоп.

### Параллельные ошибки

Если ошибок несколько — можно по очереди. Порядок:

1. Структурный (С)
2. Логический (Л)
3. Мета (М)
4. Процедурный (П)
5. Данные (Д)
6. UX (У)
7. Приоритеты (Пр)
8. Внешний (В)
9. Память (Пм)

---

## II. КЛАССЫ ОШИБОК · ПО ПРИЧИНЕ

Девять классов. Каждый — по причине, не по симптому.

| Класс | Код | Причина | Признак |
|-------|-----|---------|---------|
| Структурный | С | Нарушена структура | Структура не соответствует ABCD |
| Логический | Л | Неверная логика | Противоречие с ядром |
| Мета | М | Ошибка метода | Повторяется, исправление не помогает |
| Процедурный | П | Нарушен порядок | Элемент не встроен в последовательность |
| Данные | Д | Устаревание / нет источника | Нет источника или нет даты |
| UX | У | Ожидание ≠ реальность | Разница между ожиданием и UI |
| Приоритеты | Пр | Неверная расстановка | Делается не тот уровень |
| Внешний | В | Отказ / угроза извне | Не наша ошибка |
| Память | Пм | Потеря / утечка | Контекст потерян или приватное ушло |

### Флаг «повторяется»

- Применяется к любому классу.
- Если класс + флаг — усиленное действие.
- Если класс без флага — обычное действие.
- Флаг — не класс, а характеристика.

**Правило усиления:**
- С + флаг → ШПО + ШКУ.
- Л + флаг → ШКУ + ШОтк.
- М + флаг → ШПО + ШППСис + ШКУ.
- П + флаг → ШУО + ШПО.
- Д + флаг → ШУО + ШПО.
- У + флаг → ШДШ + ШПО.
- Пр + флаг → ШУО + ШКУ.
- В + флаг → ШЗащ + ШПО.
- Пм + флаг → ШУО + ШЗащ + ШПО.

---

## III. ДЕЙСТВИЯ · ПО КЛАССУ

Восемь действий.

| Действие | Код | Когда | Что делает |
|----------|-----|-------|------------|
| Упреждение | ШУО | Профилактика | Помогает не допустить |
| Пересборка | ШПО | Шаблон не сработал | Пересобрать |
| Классификация | ШКО | Причина неясна | Определить класс |
| Корректировка уровня | ШКУ | Не хватает слоя | ABCD+ |
| Добавление шаблона | ШДШ | Нужен новый | Создать |
| Сообщение | ШСообщСист | Решения нет | Сказать прямо |
| Откат | ШОтк | Решение неверное | Вернуть решение |
| Защита | ШЗащ | Внешний риск / утечка | Fallback / изоляция |

### Матрица «класс → действие»

| Класс | Основное действие |
|-------|-------------------|
| С | ШПО |
| Л | ШКУ |
| М | ШПО + ШППСис |
| П | ШУО |
| Д | ШУО |
| У | ШДШ |
| Пр | ШУО |
| В | ШЗащ |
| Пм | ШУО + ШЗащ |

### Детали действий

**ШОтк (Откат).**
- Объект: решение.
- Условие: решение принято и оказалось неверным.
- Частота: 1 раз.
- Процедура: вернуть решение к предыдущей точке.
- Если после отката то же решение отвергается снова — перейти к ШПО.

**ШЗащ (Защита).**
- Классы: В и Пм.
- Условия:
  - В: отказ провайдера, отказ сети, внешняя угроза.
  - Пм: риск утечки, риск потери контекста.
- Процедуры:
  - fallback: несколько провайдеров.
  - изоляция: падение одного не рушит других.
  - шифрование: приватное.
  - мониторинг: отслеживать отказы.

### Граница «откат / повтор»

- **ШОтк** — если решение было принято и оказалось неверным. 1 раз.
- **ШПО** — если ошибка системная (2+ раза). Усиленно.

---

## IV. ЕДИНЫЙ АЛГОРИТМ ПРОФИЛАКТИКИ

**Шаг 1 · Определить класс задачи.** К какому из 9 классов относится?

**Шаг 2 · Проверить условия класса.**
- Данные — есть источник?
- Структура — ABCD соблюдён?
- Логика — согласуется с ядром?
- UX — что ждёт пользователь?
- Приоритеты — уровень 1, 2 или 3?
- Процедура — встроено в порядок?
- Внешний — есть защита?
- Память — сохранено? зашифровано?

**Шаг 3 · Различать уровни.** Сначала — работает. Потом — функции. Потом — красота.

**Шаг 4 · Если сомнение — ШСообщСист.** Сказать прямо: «не знаю». Дать выбор.

**Шаг 5 · Проверить после.** Прогнать ШППК. Прогнать ШПров.

**Шаг 6 · Записать.** В реестр. В learning.

**Стоп-условие.** Класс не определён → стоп. Условия не проверены → стоп.

---

## V. ПРАВИЛА ПО КЛАССАМ

### Класс С · Структурный

**Причина:** нарушена структура.
**Признак:** структура не соответствует ABCD.
**Решение:** ШПО — пересобрать через ABCD × ABCD.

**Примеры:**
- Приоритеты списком → ABCD × ABCD.
- Принципы 5 → 16.
- Карточка 3 действия → 4.
- Шаблон без ABCD → ABCD заполнен.

**В такой ситуации можно иначе:** список — поверхность; ABCD — структура.

### Класс Л · Логический

**Причина:** неверная логика.
**Признак:** противоречие с ядром.
**Решение:** ШКУ.

**Примеры:**
- A = данные → A = вход И факт.
- Оформление в A → в D.
- 4 файла vs 16 фрагментов → разные масштабы.

**В такой ситуации можно иначе:** проверить, как элемент согласуется с ядром.

### Класс М · Мета

**Причина:** ошибка метода.
**Признак:** повторяется, исправление не помогает.
**Решение:** ШПО + ШППСис.

**Примеры:**
- Додумывание → ШСообщСист.
- Слепые проверки → ШПЭфф + ШППСис.
- Линейный прогноз → экспонента.
- Заявление «0 ошибок» без процедуры → провести процедуру.

**В такой ситуации можно иначе:** признать, что решение пока не найдено; поднять уровень.

### Класс П · Процедурный

**Причина:** нарушен порядок.
**Признак:** элемент не встроен в последовательность.
**Решение:** ШУО — встроить в порядок.

**Примеры:**
- ШМЗ не встроен → встроен.
- Проверки не применялись → применяются.

**В такой ситуации можно иначе:** соединить элемент с местом в последовательности.

### Класс Д · Данные

**Причина:** устарели / нет источника.
**Признак:** нет источника или нет даты.
**Решение:** ШУО — проверка источника.

**Что относится к данным:** модели, URL API, лимиты, версии, ставки, цены, курсы, имена, роли, сроки, числа из внешнего мира.

**Правила класса Д:**

1. **Перед использованием** — открыть первоисточник. Сверить. Зафиксировать дату.
2. **Указание источника — условие класса.** Без источника данные не считаются проверенными.
3. **Формат** — можно указать так: «по данным X от [дата]». Формат свободный, суть — источник + дата.
4. **Память может устаревать.** «Я помню» — не источник.
5. **Если данных нет** — сообщить.

**В такой ситуации можно иначе:** проверить источник, дать факт с датой.

### Класс У · UX

**Причина:** ожидание ≠ реальность.
**Признак:** разница между ожиданием и UI.
**Решение:** ШДШ — добавить или исправить.

**Правила:**

1. Определить ожидания.
2. Сверить с реальностью.
3. Разница — сигнал.
4. Проверить по списку: копирование, сохранение, промпты, тема, провайдер, BYOK.
5. Если нет — добавить.

**В такой ситуации можно иначе:** посмотреть на систему глазами пользователя.

### Класс Пр · Приоритеты

**Причина:** неверная расстановка.
**Признак:** делается не тот уровень.
**Решение:** ШУО — вернуться к порядку.

**Порядок:** 1 — работает; 2 — функции; 3 — красота.

**В такой ситуации можно иначе:** сначала основа, потом украшение.

### Класс В · Внешний

**Причина:** отказ / угроза извне.
**Признак:** не наша ошибка — сервис, сеть, конкуренты.
**Решение:** ШЗащ.

**Правила:**

1. Fallback: несколько провайдеров.
2. Изоляция: падение одного не рушит других.
3. Шифрование: если утечка.
4. Мониторинг: если отказ.

**Примеры:**
- Провайдер вернул 429 → fallback на другого.
- Провайдер недоступен → следующий в chain.
- Атака / утечка → изоляция + шифрование.

**Новый класс (2026-09-21).**

**В такой ситуации можно иначе:** держать несколько опор.

### Класс Пм · Память

**Причина:** потеря / утечка.
**Признак:** контекст потерян или приватное ушло.
**Решение:** ШУО (сохранение) + ШЗащ (шифрование).

**Правила:**

1. Сохранять каждый диалог. Формат {id, title, messages}.
2. Хранить в файле / базе.
3. Шифровать приватное.
4. При старте — загружать.
5. Передача контекста между сессиями — через файл Monolog.md.
6. Право на забвение — в любой момент.

**Примеры:**
- CONTEXTS = [] в памяти → потеря при перезапуске.
- Приватное ушло в модель → разделение слоёв.

**Новый класс (2026-09-21).**

**В такой ситуации можно иначе:** держать память рядом, а не в процессе.

---

## VI. СИТУАЦИИ · ПОРЯДОК ДЕЙСТВИЙ

### Ситуация 1 · Поступили новые данные

1. Вход (что, от кого, мерность).
2. ВП (тип, источник, мерность, полнота).
3. Согласование с ядром.
4. **Проверка на актуальность (класс Д): модели, URL, лимиты, версии, цены.**
5. Классификация.
6. Разбор (если 2+ — ШМЗ).
7. Обработка по ABCD.
8. Проверка (ШППК, ШПров, ШПРек).
9. Вывод.
10. Запись.

### Ситуация 2 · Обнаружена ошибка

Применяется раздел I.

### Ситуация 3 · Тупик

1. Заметить, чего не хватает.
2. Поднять уровень.
3. Сообщить.

### Ситуация 4 · Ротация и BYOK

1. Ротация ключей: round-robin, cooldown при 429.
2. Ротация провайдеров: fallback chain.
3. BYOK: свои ключи — приоритет; хранить шифрованно.

### Ситуация 5 · Контекст и файлы

1. Контекст: сохранять; {id, title, messages}.
2. Отправка: последние 20 + system-промпт.
3. Файлы: до 3 файлов, до 3000 символов.
4. Длинные: мягкий 16K, жёсткий 32K; маршрутизация по длине.

### Ситуация 6 · Перед деплоем

1. Проверка данных (класс Д).
2. Проверка ключей.
3. Проверка структуры.
4. Проверка интерфейса.
5. Локальный тест.
6. После деплоя: логи, роуты, UI.
7. При ошибке: traceback → класс → фикс → передеплой → protocol.

### Ситуация 7 · Общение

1. Тон.
2. Формат.
3. Ожидания.
4. Благодарность.
5. Индекс.

### Ситуация 8 · Шаблоны

1. Создание: 2+ / 3+ / стабильно / обобщается.
2. ABCD.
3. Метаданные.
4. Проверка: 3 применения, ≥0.7.
5. Встраивание.
6. Статус.

### Ситуация 9 · Концепция

1. Ядро — устойчиво.
2. Обвязка — меняется.
3. Концепция — живая.
4. История версий.

### Ситуация 10 · Повторяющаяся ошибка

Применяется класс М (ШПО + ШППСис) + ШКУ.

---

## VII. ИСТОЧНИКИ

| Правило | Источник |
|---------|----------|
| Файлы — до 3, до 3000 символов | app.py (MAX_ATTACHMENTS, MAX_ATTACH_LEN) |
| Cooldown — 65 сек | стандарт rate-limit |
| Мягкий лимит 16K, жёсткий 32K | концепция v9 (раздел III, ABCD-прогноз, работа с длинными текстами) |
| 20 сообщений в контексте | data_chat.py resolve_context |
| Провайдеры: groq, openrouter, cerebras, sambanova | data_chat.py PROVIDERS |

При изменении источника — обновить правило.

---

## VIII. ОРИЕНТИРЫ

Вместо «не делай» — «в такой ситуации можно иначе».

1. Додумывать → можно сообщить, что не знаешь.
2. Обещать сроки → можно показать факты.
3. Давить соматикой → можно подсветить.
4. Навязывать → можно дать выбор.
5. Оценивать → можно отражать.
6. Защищать идею → можно строить продукт.
7. Утверждать без источника → можно проверить.
8. Повторять ошибку → можно записать в protocol.
9. Смешивать фикс и протокол → можно разделить.
10. Выдумывать структуры → можно использовать ABCD.
11. Использовать старые данные → можно проверить источник.
12. Делать красоту раньше работы → можно сначала основу.
13. Оставлять шаблон без ABCD → можно заполнить ABCD.
14. Смешивать ядро и обвязку → можно различать.
15. Повторять ошибку дважды → можно встроить правило.
16. Полагаться на одного провайдера → можно держать несколько.
17. Хранить в памяти процесса → можно хранить в файле / базе.
18. Писать «0 ошибок» без процедуры → можно провести процедуру.
19. Заявлять «готов к применению» без процедуры → можно провести проверку.
20. Путать мета-вывод с выводом → можно различать уровни.

---

## IX. РЕЕСТР ПРИНЯТЫХ РЕШЕНИЙ

| # | Решение | Зачем |
|---|---------|-------|
| Р-1 | Гибрид: MVP + потом фрактальность | «Сразу правильно» не сработало |
| Р-2 | A = точка входа И факт | ПР-001 |
| Р-3 | chat_meta_fallback — минимальный JSON | Устойчивость |
| Р-4 | Merge dev → release: авто + подтверждение | ПР-017 |
| Р-5 | layer_a_content: локально + копия | ПР-018 |
| Р-6 | ABCD × ABCD для принципов и приоритетов | Устраняет списки |
| Р-7 | Ядро устойчиво, обвязка адаптивна | Концепция v9 |
| Р-8 | 4 сервиса | Изоляция |
| Р-9 | Ротация ключей + cooldown | Лимиты |
| Р-10 | BYOK | Открытость |
| Р-11 | Контекст — файл, потом база | MVP |
| Р-12 | Файлы — upload + ссылка | Длинные тексты |
| Р-13 | Маршрутизация по длине | Лимиты |
| Р-14 | Не додумывать — ШСообщСист | Ядро |
| Р-15 | Правило вместо разбора | Универсальность |
| Р-16 | Протокол — про порядок, данные — отдельно | Разделение |
| Р-17 | Приоритеты: работает → функции → красота | Основа первая |
| Р-18 | Шаблон без применения — не шаблон | Дисциплина |
| Р-19 | Повтор — пересмотр класса | Системность |
| Р-20 | Тупик — сигнал о нехватке | Поднятие уровня |
| Р-21 | 9 классов, 8 действий | Универсальность |
| Р-22 | Флаг «повторяется» вместо класса Сис | Чистота |
| Р-23 | Откат — 1 раз; повтор — 2+ | Граница |
| Р-24 | Параллельные ошибки — по приоритету классов | Порядок |
| Р-25 | Источник цифр — в отдельном разделе | Прослеживаемость |
| Р-26 | Не писать «0 ошибок» без процедуры | Честность |
| Р-27 | Язык естественного потока | Согласовано с ядром |
| Р-28 | Признак класса — один; примеры — отдельно | Чистота |
| Р-29 | Источник — условие класса Д; формат свободный | Обязательность |
| Р-30 | ШОтк откатывает решение; ШЗащ — для В и Пм | Ясность |

---

## X. МЕТА

**Что такое мета этого протокола:**

Протокол — не список правил. Протокол — система принятия решений в ситуации. Он не предписывает. Он описывает, как думать, когда что-то происходит.

**Мета-уровни:**

- **Уровень 0** — принципы (ядро).
- **Уровень 1** — классы ошибок.
- **Уровень 2** — действия.
- **Уровень 3** — правила.
- **Уровень 4** — ситуации.
- **Уровень 5** — мета.

**Мета-правило:** если ситуация не описана — можно поднять уровень. Сказать прямо. Дать выбор.

**Мета-вывод:**

Протокол v7 прошёл проверку. Язык — естественный поток. Ограничения сняты. Выбор — за человеком.

Проверка проведена фактически:
- разборка на ABCD.
- углубление на ABCD.
- проверка на противоречия.
- корректоры KA/KB/KC/KD.
- сборка.
- мета-корректор.

Протокол — рабочая система. Обновляется по мере новых ситуаций.

---

## XI. ИСТОРИЯ ВЕРСИЙ

- v1 · 2026-09-21 · создан.
- v2 · 2026-09-21 · убраны разборы конкретных ошибок.
- v3 · 2026-09-21 · сведено к классам.
- v4 · 2026-09-21 · 9 классов, 7 действий, флаг, границы.
- v5 · 2026-09-21 · 8 действий; правило «не формула, а процедура».
- v6 · 2026-09-21 · язык естественного потока.
- v7 · 2026-09-21 · признак — один на класс; примеры — отдельно; источник — условие класса Д; ШОтк и ШЗащ описаны; примеры для В и Пм; убрано «готов к применению»; исправлены источники.

---

## XII. ЧТО ДАЛЬШЕ

- Обновлять после каждой ошибки.
- Обновлять после каждого изменения метода.
- Ревизия — раз в неделю.
- Пробелы в классах — дополнять.
- Мета-уровень — пересматривать раз в месяц.
- Язык — держать в естественном потоке.

---

## КОНЕЦ ФАЙЛА