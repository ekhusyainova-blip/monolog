# STRUCTURE — фрактальная структура Monolog

Версия 0.1. Живой документ. Дополняется по ходу.

---

## 1. Общая идея

Структура фрактальная. **A/B/C/D повторяются на всех уровнях:**

- Уровень папок.
- Уровень файлов.
- Уровень фрагментов.
- Уровень мини-фрагментов.

**Каждый файл делает один запрос**, а не четыре. Четыре фрагмента складываются в один запрос.

**С каждым запросом отправляется второй МЕТА-запрос.**

---

## 2. Папки

- **A ПАПКА — ЗАПРОС.** Формирование запросов.
- **B ПАПКА — БАЗОВЫЙ ИНТЕРФЕЙС.** Базовое отображение.
- **C ПАПКА — МОДУЛИ ИНТЕРФЕЙСА.** Модули.
- **D ФАЙЛ — МЕТА.** Мета-информация.

---

## 3. A ПАПКА — ЗАПРОС

### A файл: данные
- A фрагмент: получение запроса или событие
- B фрагмент: запрос интерпретации
- C фрагмент: запрос решения
- D фрагмент: запрос результата

Единый запрос A,B,C,D в ответ на любое событие.
С запросом отправляется второй МЕТА-запрос.
Вывод: в B файл A папки.

### B файл: коннектор (куда выводить)
- A фрагмент: запрос фактов
- B фрагмент: запрос интерпретации
- C фрагмент: запрос решения
- D фрагмент: запрос результата

Единый запрос в ответ на любое событие.
Запрос содержит вывод из A файла B папки.
Вывод: в C файл A папки.

### C файл: решение (что выводить)
- A фрагмент: запрос фактов (триггер вывода результата)
- B фрагмент: запрос интерпретации
- C фрагмент: запрос решения
- D фрагмент: запрос результата

Единый запрос в ответ на любое событие.
Запрос содержит вывод из A и B файла B папки.
Вывод: в D файл A папки.

### D файл: запрос вывода
- A фрагмент: запрос фактов
- B фрагмент: запрос интерпретации
- C фрагмент: запрос решения
- D фрагмент: запрос результата

Единый запрос в ответ на любое событие.
Запрос содержит вывод из A, B и C файла B папки.
Вывод: в файл, пользователю или мне.

---

## 4. B ПАПКА — БАЗОВЫЙ ИНТЕРФЕЙС

**В каждом файле три служебных элемента:**

- **Отключение базового функционала.**
- **Фильтр базового функционала.**
- **Заглушка фрагментов.**

**В каждом файле по два экземпляра каждого фрагмента:**

- A фрагмент: переменные и значения
- A фрагмент: переменные и значения (фикс)
- B фрагмент: получает условия и события
- B фрагмент: получает условия и события (фикс)
- C фрагмент: получает решение и действует
- C фрагмент: получает решение и действует (фикс)
- D фрагмент: получает и выводит результат
- D фрагмент: получает и выводит результат (фикс)

### A файл: данные
Вывод: в B файл A папки.

### B файл: коннектор (условия и события)
Вывод: в C файл A папки.

### C файл: решение (комплект в обёртке)
Вывод: в D файл A папки.

### D файл: результат
Вывод: в файл, пользователю или мне.

---

## 5. C ПАПКА — МОДУЛИ ИНТЕРФЕЙСА

Те же четыре файла: A, B, C, D.
Те же три служебных элемента.
Те же фрагменты, обычные и фикс.

**Вывод:** в A файл A папки, пользователю или мне.

---

## 6. D ФАЙЛ — МЕТА

Те же четыре файла: A, B, C, D.
Те же три служебных элемента.
Те же фрагменты.

**Вывод:** в файл, пользователю, мне или всем.

**Правило вывода всем:**

- Результат сохраняется в хранилище.
- Уходит в качестве МЕТА-запроса мне.
- Вместе со следующим моим запросом.
- После моего подтверждения.
- После подтверждения Безопасности.
- Уходит на адаптацию всем пользователям или отдельным группам.

---

## 7. Фикс

**Фикс — предзаполненные значения.** Не ИИ заполняет. **Значения не меняются.**

Если значения **надо поменять** — **переход в другой файл**.

**Всё, кроме фикс — унифицированные шаблоны.**

---

## 8. Единый запрос

**Не четыре запроса, а один.** Четыре фрагмента складываются в один запрос.

**С запросом отправляется второй МЕТА-запрос.**

---

## 9. МЕТА-запрос

**Второй запрос**, который идёт вместе с основным.

**Содержит:** служебную информацию.

**Используется:**
- Для сохранения в хранилище.
- Для подтверждения.
- Для отправки всем пользователям или группам.

---

## 10. Принципы

- Файл ≤ 80 строк.
- Файл независим.
- Файл — только события.
- Файл — слои A/B/C/D внутри.
- Файл — фикс + обычные фрагменты.
- Файл — три служебных элемента: отключение, фильтр, заглушка.

---

## Статус

- Версия: 0.1
- Дата: 2026-09-18
- Обновляется по ходу разработки